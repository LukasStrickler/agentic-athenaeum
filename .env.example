# Copy to .env and fill in as needed.
# Place at repo root (for dev/tests) or project root (for 3rd-party), or use the
# global config file instead. See docs/environment-variables.md and docs/setup-guide.md.

# Skills CLI: opt out of anonymous telemetry (optional)
# DISABLE_TELEMETRY=1

# Example placeholders for skills that need API keys (add when skills require them)
# OPENAI_API_KEY=
# API_BASE_URL=
